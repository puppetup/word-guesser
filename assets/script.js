//List of words
var guessWord = ['apple','banana','orange','grapes'];

// Convert word to array
var testWord = 'apple'

var appleArray = testWord.split('')

console.log(appleArray)

// Convert array to underscores
var arrayUnderscore = appleArray.map((element) => {
    return element = '_'
})

console.log(arrayUnderscore)

// Check if value is in original array
if appleArray.includes(

    //key value

) 

// replace 
appleArray.forEach(matchLetter)

function matchLetter(keyValue, element) {

    if (keyValue === element) {

        // note the index of matching element
        // replace underscore with the same index in arrayUnderscore with corresponding letter
        // display on screen
        // continue replacing underscores if correct key is pressed

    }

}